name: Test Checkout Action

on: [push]

jobs:
  test:
    runs-on: codebuild-test-check-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Check Node version
        run: |
          node --version
          ldd --version
      
      - name: Checkout code
        uses: actions/checkout@v5
